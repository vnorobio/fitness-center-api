spring:
  application:
    name: fitness-center-api
  liquibase:
    enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/black_mamba
    username: postgres
    password: m4mb4
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
